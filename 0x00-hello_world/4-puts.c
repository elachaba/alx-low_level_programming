#include <stdio.h>

/*
 * main - Prints a string to the stdout.
 *
 * Return: Always 0
*/
int main(void)
{
	char str[52] = "\"Programming is like building a multilingual puzzle";

	puts(str);
	return (0);
}
